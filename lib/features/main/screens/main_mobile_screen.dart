import 'package:flutter/material.dart';

class MainMobileScreen extends StatelessWidget {
  const MainMobileScreen({super.key, required this.content});

  final Widget content;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
